export async function GET() {
  return Response.json({
    source: "mock",
    message: "Auth route stubbed for local foundation work.",
  });
}

export async function POST() {
  return Response.json({
    source: "mock",
    message: "Auth route stubbed for local foundation work.",
  });
}
